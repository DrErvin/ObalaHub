const MessageForm = () => {
    const handleSubmit = () => {
        
    }
    return(
        <form className="messageForm" onSubmit={handleSubmit}>

        </form>
    )
}

export default MessageForm;